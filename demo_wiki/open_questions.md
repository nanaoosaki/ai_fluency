# Open Questions

- Which extraction errors are caused by ambiguous source material rather than model behavior?
- Which fields should be optional until more examples are reviewed?
- What is the smallest evaluation set that surfaces repeated failure patterns?
