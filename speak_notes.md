# Speaker Notes: Deferred

Speaker notes are intentionally not part of the active authoring workflow yet.

The current drafting priority is:

1. clarify the story in `STORYBOARD.md`;
2. refine visible slide text in `index.html`;
3. rehearse freely from the visible conceptual anchors;
4. add short presenter cues only if rehearsal exposes a real need.

The static presentation currently contains no `<aside class="notes">` content.
